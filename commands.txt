Naming Scheme - verb - noun

Tele-OP

//Drivetrain
//- DriveArcade - const
//- DriveMotionProfile

//Climber
//- DeployClimberPosition - press
//- DeployClimberManual - hold
//- WinchClimber - hold

//CP
//- ToggleControlPanel - press
//- RotateControlPanel(double speed bool manual)
//- SpinControlPanelCount(int numSpins)
//- SpinToColor - dashboard

Intake
//- CollectBall - hold
//- ToggleCollectorDeploy - Dashboard
//- LoadStage1 - press
//- LoadStage2 - press
//- Shoot - hold

Turret
//- SetShooterSpeed(double speed) - hold
//- RotateHood(double speed) - hold
//- SetHoodPosition(double degrees) (uses PID)
//- RotateTurret(double speed) - hold
//- SetTurretPosition(double degrees) - press
//AlignTurret

Autonomous Commands
    CollectBallWithVision
        // drive a maximum distance or until it sees a ball switch (whichever comes first)
        // if ball not found with vision, set turn = 0
    CollectBallsWithVision(int numBalls)
        // commandGroup that creates and runs the specified number of CollectBallWithVision commands
    ShootAndReload(boolean useVision)
        // CommandGroup that runs Shoot, LoadStage2, LoadStage1
        // if useVision is true, and visionTarget is not in the frame, it exits without doing anything

Auto #1 - Drive Forward, shoot 3, collect 5 at CP, shoot 5, collect 1 at RZ, shoot 1
    // Spin Up Shooter
    SetShooterSpeed
    
    // Drive Forward  
    DriveMotionProfile

    // line up and shoot 3 
    AlignTurretVision
    ShootAndReload(true)
    ShootAndReload(true)
    ShootAndReload(true)

    //drive to Control Panel  
    DriveMotionProfile

    //collect 5 balls 
    CollectBallsWithVision(int numBalls = 5)

    //drive to Goal
    DriveMotionProfile

    //line up and shoot 
    AlignTurretVision
    ShootAndReload(true)
    ShootAndReload(true)
    ShootAndReload(true)
    ShootAndReload(true)
    ShootAndReload(true)

    //drive to Rendezvous 
    DriveMotionProfile

    //collect 1 ball 
    CollectBallWithVision

    //drive to Goal
    DriveMotionProfile

    //line up and shoot
    AlignTurretVision
    ShootAndReload(true)


sensor 1 hit -> spin m1 & m2
sensor 3 hit -> stop m2
sensor 3 and 2 hit -> stop m1
sens 3 2 and 1 hit -> stop Intake
sens 3 free -> repeat